package assignmentforprogsemester2;

public class TestPackage {

    /*
    public static void TestSaveStudent() {
        Students testStudent = new Students("1234", "TestName", "20", "test@email.com", "TestCourse");
        saveStudent(testStudent);
        boolean isSaved = studentDetails.stream().anyMatch(student -> student.getId().equals("1234"));
        System.out.println("TestSaveStudent: " + (isSaved ? "PASSED" : "FAILED"));
    }

    public static void TestSearchStudent() {
        Students foundStudent = studentSearch("1234");
        boolean testPassed = foundStudent != null && foundStudent.getId().equals("1234");
        System.out.println("TestSearchStudent: " + (testPassed ? "PASSED" : "FAILED"));
    }

    public static void TestSearchStudent_StudentNotFound() {
        Students notFoundStudent = studentSearch("9999");
        boolean testPassed = notFoundStudent == null;
        System.out.println("TestSearchStudent_StudentNotFound: " + (testPassed ? "PASSED" : "FAILED"));
    }

    public static void TestDeleteStudent() {
        boolean isDeleted = deleteStudentById("1234");
        boolean testPassed = !studentDetails.stream().anyMatch(student -> student.getId().equals("1234"));
        System.out.println("TestDeleteStudent: " + (isDeleted && testPassed ? "PASSED" : "FAILED"));
    }

    public static void TestDeleteStudent_StudentNotFound() {
        boolean isDeleted = deleteStudentById("9999");
        System.out.println("TestDeleteStudent_StudentNotFound: " + (!isDeleted ? "PASSED" : "FAILED"));
    }

    public static void TestStudentAge_StudentAgeValid() {
        Students s = new Students();
        s.setAge("20");
        boolean testPassed = s.getAge().equals("20");
        System.out.println("TestStudentAge_StudentAgeValid: " + (testPassed ? "PASSED" : "FAILED"));
    }

    public static void TestStudentAge_StudentAgeInvalid() {
        Students s = new Students();
        s.setAge("10");
        boolean testPassed = s.getAge() == null;
        System.out.println("TestStudentAge_StudentAgeInvalid: " + (testPassed ? "PASSED" : "FAILED"));
    }

    public static void TestStudentAge_StudentAgeInvalidCharacter() {
        Students s = new Students();
        s.setAge("abc");
        boolean testPassed = s.getAge() == null;
        System.out.println("TestStudentAge_StudentAgeInvalidCharacter: " + (testPassed ? "PASSED" : "FAILED"));
    }

    */
    
    
}
